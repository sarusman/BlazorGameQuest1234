server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA: sert index.html pour toute route non-fichier
  location / {
    try_files $uri $uri/ /index.html;
  }
}
